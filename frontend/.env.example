# Local dev: talk directly to Fastify backend
NEXT_PUBLIC_API_URL=http://localhost:5001/api/v1

# Vercel only: Render/Railway backend URL (used by next.config rewrites — not exposed to browser)
# BACKEND_URL=https://your-backend.onrender.com
